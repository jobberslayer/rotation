<% provide(:title, "Edit Volunteer") %>
<div>
  <%= form_for(@volunteer) do |v| %>
  <%=render 'shared/error_messages', obj: @volunteer%>
  <fieldset>
    <legend>Edit Volunteer</legend>
    <div class="span2">
      <%= v.label :first_name %>
      <%= v.text_field :first_name %>

      <%= v.label :last_name %>
      <%= v.text_field :last_name %>

      <br/>
      <%= v.submit "Save changes", class: "btn btn-large btn-primary" %>
    </div>
  </fieldset>
  <%end%>
</div>
